#!/usr/bin/env zsh

if [ "$HOST" = "badura" ] || [ "$HOST" = "boxy" ]; then
	waybar &
	swww init &

	sleep 2

	swww img $(random-wallpaper) &
	easyeffects --gapplication-service &

	firefox &

	sleep 2

	alacritty --class ncspot --command ncspot &

	sleep 2

	signal-desktop &
fi
